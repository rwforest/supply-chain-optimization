[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ka-chat-bot"
version = "0.1.0"
description = "Databricks Chat Bot Application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0,<1.0.0",
    "uvicorn>=0.24.0",
    "pydantic>=2.5.0",
    "python-dotenv>=1.2.2",
    "databricks-sdk>=0.62.0",
    "aiofiles>=23.2.1",
    "backoff>=2.2.1",
    "gunicorn>=23.0.0",
    "httpx>=0.25.2",
    "sqlalchemy>=2.0.42",
    "asyncpg>=0.29.0",
    "greenlet>=2.0.0",
    "langgraph-checkpoint-postgres>=2.0.23",
    "psycopg>=3.2.9",
    "psycopg-binary>=3.2.9",
    "psycopg2-binary>=2.9.10",
    "psycopg-pool>=3.2.6",
    "langchain>=1.0.0",
    "langchain-community>=0.4.0",
    "langgraph>=1.0.0",
    "langgraph-checkpoint>=4.0.0",
    "langchain-openai>=1.0.0",
    "langchain-text-splitters>=1.1.0",
    "databricks-langchain>=0.19.0",
    "mlflow[databricks]>=3.5.0,<4.0.0",
    "pyomo>=6.9.2",
    "highspy>=1.10.0",
    "uuid>=1.30",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=0.21.0",
    "ruff>=0.1.0",
    "black>=26.3.1",
]
# GPU-only group for the `09_mps_cuopt_pipeline` notebook's FJSP/CVRPTW cuOpt
# solves. NOT part of the default install and deliberately NOT folded into
# requirements.txt (which every other notebook installs): these wheels require
# a Linux/WSL2 host with a CUDA 12+ GPU (NVIDIA Volta+, compute capability
# >=7.0) and must be installed from NVIDIA's index, e.g.:
#   pip install --extra-index-url=https://pypi.nvidia.com -r requirements-gpu.txt
# See requirements-gpu.txt and docs/mps-cuopt-pipeline.md. This combination
# (cuOpt on Databricks serverless GPU) is technically feasible but not
# officially supported/documented by Databricks or NVIDIA as of this writing.
gpu = [
    "nvidia-cuda-runtime-cu12==12.9.*",
    "cuopt-cu12==26.6.*",
    "cudf-cu12",
]

[tool.ruff]
target-version = "py312"
line-length = 88
select = ["E", "F", "I", "N", "W", "B", "C4", "UP"]
ignore = ["E501", "B008"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
    "uuid>=1.30",
]
